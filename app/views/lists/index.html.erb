<% @lists.each do |list| %>
  <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path list.photo.key, height: 300, width: 400, crop: :fill %>')"><%= link_to list.name, list_path(list.id) %></div>
  <%#= cl_image_tag list.photo.key, height: 300, width: 400, crop: :fill %>
<% end %>

<%= link_to "Create a list", new_list_path, class:"btn btn-primary" %>
